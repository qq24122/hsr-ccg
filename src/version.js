/* 构建版本戳。
 *
 * 这个值由 deploy.pl 在每次部署时自动改写（内容取自 git 的短哈希与日期）。
 * 战绩上报必须带上它——否则一次平衡改动前后的数据会混在一起，
 * 「某套卡组胜率 45%」这种结论就无从判断是改之前还是改之后的了。
 *
 * 手工改这个文件没有意义，会被下次部署覆盖。 */
export const BUILD = {
  id:   '20260818.33',            // git 短哈希，本地未部署时是 dev
  date: '2026-08-18',     // 部署日期
  cards: 363,             // 当次部署的卡牌总数，便于粗略核对
};
